"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var env_exports = {};
__export(env_exports, {
  env: () => env
});
module.exports = __toCommonJS(env_exports);
var import_commander = require("commander");
var import_context = require("../bundler/context.js");
var import_api = require("./lib/api.js");
var import_utils = require("./lib/utils.js");
var import_version = require("./version.js");
var import_chalk = __toESM(require("chalk"), 1);
var import_run = require("./lib/run.js");
const envSet = new import_commander.Command("set").arguments("<name> <value>").configureHelp({ showGlobalOptions: true }).action(async (name, value, _options, cmd) => {
  const options = cmd.optsWithGlobals();
  await (0, import_utils.ensureHasConvexDependency)(import_context.oneoffContext, "env set");
  await callUpdateEnvironmentVariables(options, [{ name, value }]);
  (0, import_context.logMessage)(import_context.oneoffContext, import_chalk.default.green(`Successfully set ${name}=${value}`));
});
const envGet = new import_commander.Command("get").arguments("<name>").configureHelp({ showGlobalOptions: true }).action(async (envVarName, _options, cmd) => {
  const ctx = import_context.oneoffContext;
  await (0, import_utils.ensureHasConvexDependency)(ctx, "env get");
  const options = cmd.optsWithGlobals();
  const deploymentSelection = (0, import_api.deploymentSelectionFromOptions)(options);
  const { adminKey, url } = await (0, import_api.fetchDeploymentCredentialsWithinCurrentProject)(
    import_context.oneoffContext,
    deploymentSelection
  );
  const envVar = await (0, import_run.runQuery)(
    import_context.oneoffContext,
    url,
    adminKey,
    "_system/cli/queryEnvironmentVariables:get",
    { name: envVarName }
  );
  if (envVar === null) {
    (0, import_context.logFailure)(
      import_context.oneoffContext,
      `Environment variable "${envVarName}" not found.`
    );
    return;
  }
  const { name, value } = envVar;
  (0, import_context.logMessage)(import_context.oneoffContext, `${name}=${value}`);
});
const envRemove = new import_commander.Command("remove").alias("rm").arguments("<name>").configureHelp({ showGlobalOptions: true }).action(async (name, _options, cmd) => {
  const options = cmd.optsWithGlobals();
  await (0, import_utils.ensureHasConvexDependency)(import_context.oneoffContext, "env remove");
  await callUpdateEnvironmentVariables(options, [{ name }]);
  (0, import_context.logMessage)(import_context.oneoffContext, import_chalk.default.green(`Successfully unset ${name}`));
});
const envList = new import_commander.Command("list").configureHelp({ showGlobalOptions: true }).action(async (_options, cmd) => {
  const ctx = import_context.oneoffContext;
  await (0, import_utils.ensureHasConvexDependency)(ctx, "env list");
  const options = cmd.optsWithGlobals();
  const deploymentSelection = (0, import_api.deploymentSelectionFromOptions)(options);
  const { adminKey, url } = await (0, import_api.fetchDeploymentCredentialsWithinCurrentProject)(
    import_context.oneoffContext,
    deploymentSelection
  );
  const envs = await (0, import_run.runQuery)(
    import_context.oneoffContext,
    url,
    adminKey,
    "_system/cli/queryEnvironmentVariables",
    {}
  );
  for (const { name, value } of envs) {
    (0, import_context.logMessage)(import_context.oneoffContext, `${name}=${value}`);
  }
});
async function callUpdateEnvironmentVariables(options, changes) {
  const deploymentSelection = (0, import_api.deploymentSelectionFromOptions)(options);
  const { adminKey, url } = await (0, import_api.fetchDeploymentCredentialsWithinCurrentProject)(
    import_context.oneoffContext,
    deploymentSelection
  );
  const client = (0, import_utils.deploymentClient)(url);
  const headers = {
    Authorization: `Convex ${adminKey}`,
    "Convex-Client": `npm-cli-${import_version.version}`
  };
  await client.post(
    "/api/update_environment_variables",
    { changes },
    {
      headers
    }
  );
}
const env = new import_utils.DeploymentCommand("env").summary("Set and view environment variables").description(
  "Set and view environment variables on your deployment\n\n  Set a variable: `npx convex env set name value`\n  Unset a variable: `npx convex env remove name`\n  List all variables: `npx convex env list`\n  Print a variable's value: `npx convex env get name`\n\nBy default, this sets and views variables on your dev deployment."
).addDeploymentSelectionOptions("Set and view environment variables on").addCommand(envSet).addCommand(envGet).addCommand(envRemove).addCommand(envList);
//# sourceMappingURL=env.js.map
